/// <reference types="cypress" />

/**
 * @type {Cypress.PluginConfig}
 */
module.exports = (on, config) => {
  // `on` используется для подписки на события Cypress
  // `config` - конфигурация, которую можно модифицировать

  return config;
}; 
